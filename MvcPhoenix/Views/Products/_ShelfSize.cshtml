@model List<MvcPhoenix.Models.ShelfMasterViewModel>

<div id="dvShelfMasterList">

    <label>Shelf Size Info</label> <label id="noprint"> | </label> @Html.ActionLink("Manage", "Index", "ShelfMaster", new { id = @ViewBag.ParentID }, null)

    <div class="table-responsive">
        <table class="table table-hover table-striped">
            <thead>
                <tr>
                    <th>Size</th>
                    <th>Unit Wt</th>
                    <th>Part #</th>
                    <th>WH</th>
                    <th>Bin/Location</th>
                    @*<th>Min Qty</th>
                    <th>Max Qty</th>*@
                    <th>Notes</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td><strong>@item.size</strong></td>
                        <td>@item.unitweight</td>
                        <td>@item.packageid</td>
                        <td>@item.warehouse</td>
                        <td>@item.bin</td>
                        <td style="width: 60%;">@item.notes</td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>