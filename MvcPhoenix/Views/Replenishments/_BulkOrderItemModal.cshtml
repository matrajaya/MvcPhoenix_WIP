@model MvcPhoenix.Models.BulkOrderItem

<div id="dialogPlaceholderModal" class="modal fade" role="dialog">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                @*<button type="button" class="close" data-dismiss="modal">&times;</button>*@
                <h4 class="modal-title">Replenishment Order Item</h4>
            </div>

            <div id="dialogPlaceholderBody" class="modal-body">

                @using (Ajax.BeginForm("SaveItem", "Replenishments", new AjaxOptions
                {
                    LoadingElementId = "dvspinneritem",
                    UpdateTargetId = "dvItemSaveResult",
                    InsertionMode = InsertionMode.Replace,
                    OnSuccess = "RefreshItemsGrid"
                }))
                {
                    <!-- DO NOT REMOVE -->
                    <input type="hidden" id="bulkorderitemid" name="bulkorderitemid" value="@Model.bulkorderitemid" />
                    <input type="hidden" id="bulkorderid" name="bulkorderid" value="@Model.bulkorderid" />

                    <div class="row">
                        <div class="col-md-6 form-group">
                            <label>Master Code</label>
                            @Html.DropDownListFor(m => m.productmasterid, Model.ListOfProductMasters, new { @class = "form-control" })
                        </div>
                        <div class="col-md-3 form-group">
                            <label>Weight</label>
                            @Html.TextBoxFor(m => m.weight, new { @class = "form-control" })
                        </div>
                        <div class="col-md-3 form-group">
                            <label>Status</label>
                            @Html.DropDownListFor(m => m.itemstatus, Model.ListOfItemStatusIDs, new { @class = "form-control" })
                        </div>
                    </div>

                    <div class="row">
                        <div class="col-md-6 form-group">
                            <label>ETA</label>
                            @Html.TextBoxFor(m => m.eta, new { @class = "form-control", @type = "date" })
                            @Html.ValidationMessageFor(m => m.eta)
                        </div>
                        <div class="col-md-6 form-group">
                            <label>Date Received</label>
                            @Html.TextBoxFor(m => m.datereceived, new { @class = "form-control", @type = "date" })
                            @*@Html.TextBoxFor(m => m.datereceived, new { @readonly = "readonly", @class = "form-control" })*@
                            @*<input class="form-control date-picker" id="datereceived" name="searchorderdate" type="date" required />*@
                            @Html.ValidationMessageFor(m => m.datereceived)
                        </div>
                    </div>

                    <div class="row">
                        <div class="col-md-12 form-group">
                            <label>Item Notes</label>
                            @Html.TextAreaFor(m => m.itemnotes, new { maxlength = 150, @class = "form-control" })
                        </div>
                    </div>

                    <div class="row">
                        <div class="col-md-6">
                            <input id="savebutton" type="submit" value="Save" class="btn btn-success btn-block form-control" />
                        </div>
                        <div class="col-md-6">
                            <button type="button" class="btn btn-danger btn-block form-control" data-dismiss="modal">Close</button>
                        </div>
                    </div>

                    <br />
                    <div class="col-md-12 text-center" id="dvspinneritem" style="display:none; font-size:large">
                        <i class="fa fa-circle-o-notch fa-5x fa-spin"></i> Saving...
                    </div>
                    <div class="col-md-12 text-center">
                        <strong><span id="dvItemSaveResult"></span></strong>
                    </div>
                }
            </div>
        </div>
    </div>
</div>

<script type="text/javascript">
    function RefreshItemsGrid() {
        url = '@Url.Action("BulkItemsList/" + @Model.bulkorderid, "Replenishments")';
        $("#dvItemsList").load(url);
        $("#savebutton").prop('disabled', true);
    }
</script>