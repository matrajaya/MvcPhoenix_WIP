@model PagedList.IPagedList<MvcPhoenix.EF.tblGHSPHSource>
@using PagedList;
@using PagedList.Mvc;

@{
    ViewBag.Title = "PH Code Search";
}

<div class="panel panel-primary">
    <div class="panel-body">
        @{
            if (Model.Count == 0 || @ViewBag.CurrentFilter == null)
            {
                <div class="col-md-12">
                    <label class="lead">No Codes Found. Try Again.</label>
                </div>
            }
            else
            {
                <label class="lead">Search Results For "@ViewBag.CurrentFilter"</label>

                <div class="table-responsive">
                    <table class="table table-hover table-striped">
                        <thead>
                            <tr>
                                <th></th>
                                <th>P &amp; H Code</th>
                                <th>Language</th>
                                <th>Statement</th>
                                <th></th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                    <tr>
                        <td>
                            <a href="#">+ Add</a>
                        </td>
                        <td data-label="PH Code">
                            @Html.DisplayFor(modelItem => item.PHNumber)
                        </td>
                        <td data-label="Language">
                            @Html.DisplayFor(modelItem => item.Language)
                        </td>
                        <td data-label="Statement">
                            @Html.DisplayFor(modelItem => item.PHStatement)
                        </td>
                        <td data-label="" style="text-align:right">
                            <a href="#">Clone</a>
                        </td>
                    </tr>
                            }
                        </tbody>
                    </table>
                </div>
                <div class="col-md-12 text-center">
                    <label>@(Model.PageCount < Model.PageNumber ? 0 : Model.PageNumber) of @Model.PageCount</label>
                    @Html.PagedListPager((IPagedList)Model, page => Url.Action("Search", new { page, sortOrder = ViewBag.CurrentSort, currentFilter = ViewBag.CurrentFilter }),
                            PagedListRenderOptions.EnableUnobtrusiveAjaxReplacing(PagedListRenderOptions.ClassicPlusFirstAndLast,
                                new AjaxOptions
                                {
                                    InsertionMode = InsertionMode.Replace,
                                    HttpMethod = "Get",
                                    UpdateTargetId = "dvSearchPHCodes"
                                }))
                </div>
            }
        }
    </div>
</div>

<div class="row">
    <div class="col-md-12">
        <i class="fa fa-arrow-circle-up"></i> <a href="#top">Back to Top</a>
        <span class="pull-right"><a href="#top">Back to Top</a> <i class="fa fa-arrow-circle-up"></i></span>
    </div>
</div>


<div id="dialogPlaceholderModal" class="modal fade" role="dialog">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h4 class="modal-title">P &amp; H Clone and Modify</h4>
            </div>
            <div id="dialogPlaceholderBody" class="modal-body">
                <label>Placeholder</label>

                @*@using (Ajax.BeginForm("CloneEdit", "GHS", new AjaxOptions
                    {
                        HttpMethod = "POST",
                        UpdateTargetId = "dialogResult",
                        OnSuccess = "UpdateProductNotesList",
                        InsertionMode = InsertionMode.Replace
                    }))
                    {
                        <div class="row">
                            <div class="col-md-6 form-group">
                                <label>P&amp;H Code</label>
                                @Html.TextBoxFor(m => m, new { @class = "form-control" })
                            </div>
                            <div class="col-md-6 form-group">
                                <label>Language</label>
                                @Html.TextBoxFor(m => m, new { @class = "form-control" })
                            </div>
                        </div>
                        <div class="row">
                            <div class="col-md-12 form-group">
                                <label>Statement</label>
                                @Html.TextAreaFor(m => m, new { @class = "form-control" })
                            </div>
                        </div>
                        <div class="row">
                            <div class="col-md-6">
                                <input type="submit" value="Save" class="btn btn-success btn-block" />
                            </div>
                            <div class="col-md-6">
                                @Ajax.ActionLink("Close", null, null, new AjaxOptions
                                {
                                    HttpMethod = "GET",
                                    OnComplete = "$('#dialogPlaceholderModal').modal('hide')"
                                }, new { @class = "btn btn-danger btn-block" })
                            </div>
                        </div>
                    }*@
            </div>
        </div>
    </div>
</div>

<script type="text/javascript">
    function UpdateGHSPHCode() {
        $("#dvLogNotes").load('@Url.Action("Clone", "GHS")');
        $("#dialogPlaceholderModal").modal('hide');
    }
</script>